export default function DashboardKanbanPage() {
  // Temporarily disabled – keeping route so old links don't 404
  return (
    <div className="flex h-full flex-col items-center justify-center text-sm text-[var(--text-dim)]">
      Kanban view is paused for now. Use the Board and All Jobs views to manage your search.
    </div>
  );
}
